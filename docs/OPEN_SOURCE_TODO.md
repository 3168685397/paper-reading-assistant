# Open-source display follow-up / 开源展示待办

## English

- The already published `v0.1.0` GitHub Release attachment uses the legacy filename `science-reader-0.1.0-chrome.zip`. The asset remains valid and is not being replaced because the published release is intentionally unchanged. New local packages use `paper-reading-assistant-0.1.0-chrome.zip`.
- Add a privacy-reviewed real product screenshot at `docs/images/hero.png` before enabling the README hero image.

Screenshot privacy checklist:

- Use only public paper content.
- Hide API keys, account information, reading history, private URLs, and personal browser data.
- Remove private tabs, bookmarks, notifications, and profile details.
- Verify that page text, the address bar, and the translation popover contain no confidential research data.

## 简体中文

- 已发布的 `v0.1.0` GitHub Release 附件仍使用历史文件名 `science-reader-0.1.0-chrome.zip`。附件仍然有效；由于本阶段明确不修改已发布 Release，因此暂不替换。新的本地构建包使用 `paper-reading-assistant-0.1.0-chrome.zip`。
- 完成隐私检查后，将真实产品截图保存为 `docs/images/hero.png`，再启用 README 主图。

截图隐私检查：

- 只使用公开论文内容。
- 隐藏 API Key、账号信息、阅读历史、私有 URL 和个人浏览器数据。
- 移除私人标签页、书签、通知和个人资料。
- 确认网页文字、地址栏和翻译悬浮卡片中不含未公开研究数据。
